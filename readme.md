# 🎄 AOC 2025 in TypeScript

## 🚀 Setup

1.  **Install Dependencies:**
    ```bash
    pnpm install
    ```

2.  **Configuration:**
    * TypeScript is configured via `tsconfig.json`.
    * Solutions go in the `src/` directory (e.g., `src/day01.ts`).
    * Input files go in the `input/` directory (e.g., `input/day01.txt`).

## ▶️ How to Run a Solution

Use the `day` script, passing the two-digit day number as an argument.

| Day | Command |
| :--- | :--- |
| **Day 1** | `pnpm day 01` |
| **Day 15** | `pnpm day 15` |
| **Day N** | `pnpm day NN` |
